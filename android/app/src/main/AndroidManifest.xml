<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 通知权限 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 闹钟权限 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- 唤醒屏幕（闹钟用） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 前台服务（后续实时推送用） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- 声明需要查询的 App 包名（替代 QUERY_ALL_PACKAGES，Google Play 合规） -->
    <queries>
        <package android:name="com.ss.android.ugc.aweme" />      <!-- 抖音 -->
        <package android:name="com.tencent.mm" />                <!-- 微信 -->
        <package android:name="com.eg.android.AlipayGphone" />   <!-- 支付宝 -->
        <package android:name="com.sina.weibo" />                <!-- 微博 -->
        <package android:name="com.tencent.mobileqq" />          <!-- QQ -->
        <package android:name="com.taobao.taobao" />             <!-- 淘宝 -->
        <package android:name="com.sankuai.meituan" />           <!-- 美团 -->
        <package android:name="me.ele" />                        <!-- 饿了么 -->
        <package android:name="com.autonavi.minimap" />          <!-- 高德地图 -->
        <package android:name="com.baidu.BaiduMap" />            <!-- 百度地图 -->
        <package android:name="com.netease.cloudmusic" />        <!-- 网易云音乐 -->
        <package android:name="com.tencent.qqmusic" />           <!-- QQ音乐 -->
        <package android:name="tv.danmaku.bili" />               <!-- 哔哩哔哩 -->
        <package android:name="com.zhihu.android" />             <!-- 知乎 -->
        <package android:name="com.ss.android.article.news" />   <!-- 今日头条 -->
        <package android:name="com.jingdong.app.mall" />         <!-- 京东 -->
        <package android:name="com.xunmeng.pinduoduo" />         <!-- 拼多多 -->
        <package android:name="com.android.settings" />
        <!-- 时钟 App 包名（各种品牌手机） -->
        <package android:name="com.android.deskclock" />
        <package android:name="com.google.android.deskclock" />
        <package android:name="com.sec.android.app.clockpackage" />
        <package android:name="com.oppo.alarmclock" />
        <package android:name="com.coloros.alarmclock" />
        <package android:name="com.hihonor.deskclock" />
        <package android:name="com.huawei.deskclock" />
        <package android:name="com.oneplus.alarmclock" />
        <package android:name="com.android.BBKClock" />
        <package android:name="com.vivo.daemonService" />
        <package android:name="com.asus.deskclock" />
        <package android:name="com.lge.clock" />
        <package android:name="com.sonyericsson.organizer" />
        <package android:name="com.meizu.flyme.clock" />
        <package android:name="com.zui.deskclock" />
        <package android:name="com.transsion.clock" />
        <package android:name="com.android.calculator2" />
        <package android:name="com.android.calendar" />
        <package android:name="com.android.camera" />
        <package android:name="com.android.gallery3d" />
        <package android:name="com.android.dialer" />
        <package android:name="com.android.mms" />

        <!-- 时钟 Intent -->
        <intent>
            <action android:name="android.intent.action.SHOW_ALARMS" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>

        <!-- 通用 Intent 查询 -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AILifeAssistant"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- 启动页 -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AILifeAssistant"
            android:configChanges="orientation|screenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 登录页 -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout" />

        <!-- 聊天主页 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout"
            android:windowSoftInputMode="adjustResize" />

        <!-- 闹钟全屏弹窗 -->
        <activity
            android:name=".action.AlarmAlertActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- 闹钟广播接收器 -->
        <receiver
            android:name=".action.AlarmReceiver"
            android:exported="false" />
    </application>

</manifest>
